const store: {
  chapter: number;
  page: number;
  markedWordsCounter: number;
  sprintScore: number;
  sprintPointsForRightAnswer: number;
  sprintProgressBar: number;
} = {
  chapter: 1,
  page: 1,
  markedWordsCounter: 0,
  sprintScore: 0,
  sprintPointsForRightAnswer: 10,
  sprintProgressBar: 0,
};

export default store;
